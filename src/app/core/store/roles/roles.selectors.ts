import { rolesFeature } from './roles.reducer';

export const {
  selectRoles,
  selectLoading,
  selectSaving,
  selectCreateError,
  selectSelectedRole,
  selectClaims,
  selectClaimsLoading,
  selectClaimSaving,
  selectClaimsError,
  selectDeletingClaimKey,
  selectFeedback,
} = rolesFeature;
